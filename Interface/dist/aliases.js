"use strict";
// Type Aliases allow defining types with a custom name (an Alias).
const car = {
    year: 2009,
    type: "Toyota",
    model: "Corolla"
};
const fullHeight = 5.5;
console.log(car);
console.log(fullHeight);
